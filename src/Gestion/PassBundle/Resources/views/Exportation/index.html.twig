{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Exportation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code export</th>
                   <th>Cameras</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('exportation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.codeExport }}</td>
           
{#<td>
        {% for cameraRange in entity.getCameraRanges %}

                        {{ cameraRange }}

                        {% endfor %}
</td>#}
                <td>
                    
                <ul>
                    <li>
                        <a href="{{ path('exportation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    
                    <li>
                        <a href="{{ path('exportation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('exportation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
