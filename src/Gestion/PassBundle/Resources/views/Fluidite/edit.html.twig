{% extends 'GestionPassBundle::layout.html.twig' %}

{% block body -%}
    <h1>Fluidite edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('fluidite') }}" class="btn ">
             <i class="icon-chevron-left icon-white"> </i>
            Back to the list
        </a>
                       
            
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
