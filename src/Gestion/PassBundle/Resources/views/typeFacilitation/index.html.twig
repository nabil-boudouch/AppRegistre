{% extends '::base.html.twig' %}

{% block body -%}
    <h1>typeFacilitation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Typefacilitation</th>
                <th>Service</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('typefacilitation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.typeFacilitation }}</td>
                <td>{{ entity.service }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('typefacilitation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('typefacilitation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('typefacilitation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
