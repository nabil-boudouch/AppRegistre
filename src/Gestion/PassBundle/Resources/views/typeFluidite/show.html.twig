{% extends '::base.html.twig' %}

{% block body -%}
    <h1>typeFluidite</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Typefluidite</th>
                <td>{{ entity.typeFluidite }}</td>
            </tr>
            <tr>
                <th>Service</th>
                <td>{{ entity.service }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('typefluidite') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('typefluidite_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
